#include <stdio.h>
#include <string.h>
#include <stdio.h>
#include "lists.h"

/**
 * print_list - prints out all elements
 * of the list list_t
 * @h: pointer to head
 *
 * Return: number of nodes
 */

size_t print_list(const list_t *h)

{
	if (h == NULL)
		printf("(nil)");

	count = 0;
	while (h)
	{
		if (str == NULL)
		printf("[0] (nil)");

		printf("[%u] %s/n" len, str);
		count++;
		{



