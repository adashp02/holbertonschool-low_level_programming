#include "main.h"

/**
 * _abs - returns absolute (positive) value of a number
 * @n: int to check
 * Return: absolute value of int
 */
int _abs(int n)


{
	if (n >= 0)

	{

	return (n);

	}

	return (-n);
}
