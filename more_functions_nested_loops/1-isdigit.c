#include "main.h"

/**
 * _isdigit - checks if character is a digit
 * @c: the character to check
 * Return: 1 if c is a digit, 0 otherwise
 */

int _isdigit(int c)

{
	return (c >= 48 && c <= 57);

}
